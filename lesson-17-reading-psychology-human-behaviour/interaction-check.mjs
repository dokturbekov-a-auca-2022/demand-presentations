import fs from 'node:fs';import path from 'node:path';import{fileURLToPath}from'node:url';const here=path.dirname(fileURLToPath(import.meta.url)),html=fs.readFileSync(path.join(here,'index.html'),'utf8');let failed=false;for(const x of ['ArrowRight','PageDown','touchstart','data-pin','data-lens','spotLeft','data-seq','data-main','data-jump']){const ok=html.includes(x);console.log(`${ok?'PASS':'FAIL'} — interaction hook: ${x}`);failed||=!ok}if(failed)process.exitCode=1;
