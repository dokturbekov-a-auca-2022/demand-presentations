import fs from 'node:fs';import path from 'node:path';const here=path.dirname(new URL(import.meta.url).pathname.replace(/^\/([A-Z]):/,'$1:'));const html=fs.readFileSync(path.join(here,'index.html'),'utf8'),script=html.match(/<script>([\s\S]*?)<\/script>/)?.[1];if(!script)throw Error('No inline script');new Function(script);console.log('PASS — JavaScript syntax');
