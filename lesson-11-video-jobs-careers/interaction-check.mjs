import fs from 'node:fs';import path from 'node:path';const here=path.dirname(new URL(import.meta.url).pathname.replace(/^\/([A-Z]):/,'$1:'));const s=fs.readFileSync(path.join(here,'index.html'),'utf8');const checks=[['check-in choices','data-check'],['job vocabulary reveal','data-job'],['workplace matching','matchCheck'],['skills activity','data-skill'],['visual video storyboard','career-video-storyboard.png'],['watching detail reveal','detailReveal'],['comprehension answers','trueGrid'],['career repair','repairGrid'],['information gap','Secret career'],['interview timer','timerBtn'],['review quiz','quizMeter'],['exit ticket','exitGrid']];let bad=false;for(const[n,t]of checks){const ok=s.includes(t);bad||=!ok;console.log(`${ok?'PASS':'FAIL'} — ${n}: ${t}`)}if(bad)process.exitCode=1;
