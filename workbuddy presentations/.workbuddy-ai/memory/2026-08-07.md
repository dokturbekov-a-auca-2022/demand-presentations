# 2026-08-07

- Fixed missing lesson UI icons in `outputs/lesson-preint2-revision-mobile.html`. Removed the external Font Awesome CDN dependency and added a self-contained CSS icon map covering all 84 icon instances / 57 icon types used by the 20-slide lesson, including toolbar/navigation, lesson sections, quiz feedback, homework, and dynamic toast icons. Regenerated the HTML, validated JavaScript and icon coverage, and confirmed correct rendering in a phone-sized Chromium preview.
- Fixed lesson photos appearing as text: all 14 original Unsplash URLs returned 404. Replaced them with verified embedded JPEG photos (with embedded SVG illustration fallback) so images render directly from the standalone HTML without remote image requests. Validated 20 slides, 14/14 embedded image data URLs, no external image URLs, valid JavaScript, and phone rendering.
- Enlarged the main lesson panel for laptop/desktop screens by reducing outer viewport padding and allowing a max width of 1540px with full available height. Preserved the full-width phone override. Validated desktop and phone screenshots, 20 slides, 14 embedded images, and JavaScript integrity.
- Reworked the lesson after feedback that grammar visuals were inconsistent and desktop content was too small. Added a consistent grammar visual system with numbered explanation cards (01-04), shared title/hint styling, and integrated two-column desktop layout: existing rules/practice on the left, explanation visual on the right; phones retain a stacked scroll layout. Replaced the earlier four appended timeline cards on slides 6-9 with: routine-vs-now axis, past-vs-future axis, Present Perfect past-to-now connector, and a three-panel count/compare/predict visual. Added desktop fullscreen stage rules for wide screens: slide card now fills the viewport edge-to-edge, typography and key panels scale up, while mobile overrides remain active. Validated Chromium at 1600x900 and 390x844, 20 slides, 4 integrated grammar layouts/visuals, zero external image URLs, and intact script tags.
- Filled remaining high-impact whitespace with seven useful teaching zones rather than decoration: objectives mission path (Notice → Choose → Use), gap-fill tense-finder + 60-second timer, speaking Ask/Listen/Report mission, production 2-minute rehearsal timer, interactive recap clue sorter, checklist progress bars, and wrap-up confidence selector. Rebuilt gap-fill desktop as a balanced exercise/helper two-column grid and made each exercise a readable card; retained stacked phone scrolling. Fixed initial zone-placement issue caused by HTML comment boundaries (recap/progress/exit briefly nested into the wrap-up slide) by moving zones using a balanced-div extraction routine. Final validation: 20 slides, seven teaching zones, zero page errors at 1600x900 and 390x844, timers/clue hints/sorting/confidence/checklist interactions passed, no external image URLs.
- Per explicit desktop 16:9 feedback, structurally recomposed slides 6-12 rather than stretching mobile layouts. Grammar slides now use full-height dashboards: rule cards distribute vertically, side visual panels fill height, and teacher cues/quick checks/signal strips occupy formerly empty areas. Gap-fill now has a 4x2 set of taller sentence cards plus a full-height tense-finder/routine/timer helper. Error correction changed from six full-width strips to a compact 2-column card grid with a right-side 'Fix it in 3 steps' illustration/strategy panel. Reading now uses a desktop left-story column (photo + passage) and right comprehension column with reading-lens prompts; phone remains stacked. Final Chromium validation at 1600x900 and 390x844: 20 slides, zero page errors, reveal buttons and tense helper interactions passed, JS syntax valid, no external image URLs.
- Final content-density refinement: reduced grammar card height while enlarging internal typography and adding concise signal/example cues; added visible base-word prompts to all eight gap-fill items (`wake`, `snow`, `go`, `love`, `be`, `there/be`, `comfortable`, `leave`); expanded error correction from 6 to 10 cards and tightened sentence-to-reveal spacing; retained continuous reading passage text with larger comprehension questions; enlarged Interview Your Partner prompts. Fixed a nesting bug where the four new error cards initially landed inside card 6, then promoted them to direct error-grid siblings. Final validation confirmed 20 slides, 8 base-word prompts, 10 direct error cards, zero page errors on desktop/phone, valid JavaScript, and working reveal interactions.
