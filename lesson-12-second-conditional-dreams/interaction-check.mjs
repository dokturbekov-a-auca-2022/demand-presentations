import fs from 'node:fs';import path from 'node:path';const here=path.dirname(new URL(import.meta.url).pathname.replace(/^\/([A-Z]):/,'$1:'));const src=fs.readFileSync(path.join(here,'index.html'),'utf8');const tests=[['interactive vocabulary','vocabFlips'],['sentence builder','builder'],['form tabs','formTabs'],['error repair','repairGrid'],['reading highlights','.article mark'],['pair questions','questionStamps'],['information gap','data-reveal'],['production timer','productionStart'],['review quiz','paintQuiz'],['exit ticket','exitStrip']];let bad=false;for(const [n,t] of tests){const ok=src.includes(t);bad||=!ok;console.log((ok?'PASS':'FAIL')+' — '+n)}if(bad)process.exitCode=1;
