import fs from 'node:fs';const h=fs.readFileSync('index.html','utf8');const checks=[['28 scenes',h.includes('const units=[')&&h.includes('units.forEach')],['teacher notes',h.includes('scenes[cur].n')],['local visuals',['assets/postcard-classroom.png','assets/revision-route.png'].every(x=>fs.existsSync(x))],['navigation',h.includes('ArrowRight')&&h.includes('touchstart')],['map and notes',h.includes('data-jump')&&h.includes('noteBtn')],['all targets',/Present simple|Present perfect|First conditional|Comparatives/.test(h)],['responsive',h.includes('@media(max-width:700px)')&&h.includes('prefers-reduced-motion')],['alt text',(h.match(/alt="/g)||[]).length>=2],['offline',!/https?:\/\//.test(h)]];for(const [n,ok] of checks)if(!ok)throw Error(`FAIL — ${n}`);console.log('PASS — Lesson 1 validation');
