import fs from 'node:fs';import path from 'node:path';const h=path.dirname(new URL(import.meta.url).pathname.replace(/^\/([A-Z]):/,'$1:')),s=fs.readFileSync(path.join(h,'index.html'),'utf8'),T=['syllableSets','stressChoices','linkGrid','intonation','script mark','startTimer','nextQ','exit'];let bad=false;for(const t of T){let ok=s.includes(t);bad||=!ok;console.log((ok?'PASS':'FAIL')+' — '+t)}if(bad)process.exitCode=1;
